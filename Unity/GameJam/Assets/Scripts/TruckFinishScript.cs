using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TruckFinishScript : MonoBehaviour
{
    Transform theTruck;
    // Start is called before the first frame update
    void Start()
    {
        theTruck = this.transform;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
